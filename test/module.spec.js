/* global describe, it, expect */
import {TagManager} from '../src/index.js';

describe('Create a TagManager instance', function() {
	it('root option should be null', function() {
		expect(TagManager).toBeDefined();
	});
});
